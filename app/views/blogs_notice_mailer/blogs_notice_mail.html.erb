<!DOCTYPE html>
  <html>
    <head>
      <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    </head>
  <body>
  <%= link_to t('view.re_time_line'), root_url %>
  <p><%= t 'blogs.blogs_notice.post_new_memories!' %></p>
  <h2><%= t 'blogs.blogs_notice.what_happened', month: @blogs_month %></h2>
  <table>
    <tr>
      <th><%= t 'blogs.index.title' %></th>
      <th><%= t 'blogs.index.event_date' %></th>
    </tr>
    <% @blogs.each do |blog| %>
    <tr>
      <td><%= blog.title %></td>
      <td><%= l blog.event_date, format: :long %></td>
      <td><%= link_to t('blogs.index.blog_detail'),  group_blog_url(@group, blog) %></td>
    </tr>
    <% end %>
  </table>
  </body>
</html>